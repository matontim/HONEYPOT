# 01 — VM Preparation

## Recommended Specs
- Ubuntu Server 22.04 LTS
- 2–4 GB RAM
- 2 CPU cores
- 20+ GB storage
- Network: Bridged mode

## Initial Setup
1. Install Ubuntu normally and create a sudo-enabled user.
2. Update the OS:

```bash
sudo apt update && sudo apt upgrade -y
